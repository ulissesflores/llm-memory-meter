# Package set used for the sealed verification (2026-08-14), Python 3.14.6 on macOS arm64.
# Informational — not part of the provenance chain, and not a full `pip freeze` of the
# author's environment: these are exactly the packages the test suite requires.
# The program itself needs NONE of them — medidor.py imports only the standard library.
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
pytest==9.0.3
