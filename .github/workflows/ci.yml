name: ci

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Integrity — the committed provenance chain must verify
        run: python make_provenance.py --verify

      - name: Full replication (measure -> tests -> reseal -> verify)
        run: python run_all.py

      - name: The program runs with no dependencies at all
        run: python medidor.py

  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install linters
        run: |
          python -m pip install --upgrade pip
          pip install ruff interrogate

      - name: Ruff format (check)
        run: ruff format --check .

      - name: Ruff lint (includes pydocstyle via D rules)
        run: ruff check .

      - name: Docstring coverage (interrogate)
        run: interrogate -vv -f 100 medidor.py run_all.py make_provenance.py
